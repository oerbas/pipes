import { ConvertRemPipe } from './convert-rem.pipe';

describe('ConvertRemPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRemPipe();
    expect(pipe).toBeTruthy();
  });
});
